# Production only — your Render (or other) API URL, no trailing slash.
# Example: https://claimflow-api.onrender.com
# Leave empty for local dev (Vite proxies /api to http://127.0.0.1:8000).
VITE_API_URL=
